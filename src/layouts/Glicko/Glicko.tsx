import React from "react";
import styles from "./Glicko.module.scss";

interface GlickoProps {}

const Glicko: React.FC<GlickoProps> = (_props) => {
  return <main className={styles.glicko}>Glicko</main>;
};

export default Glicko;
